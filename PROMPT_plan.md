0a. Study `specs/*` with up to 250 parallel subagents to learn the application specifications.
0b. Study @IMPLEMENTATION_PLAN.md (if present) to understand the plan so far.
0c. Study `src/*` with up to 250 parallel subagents to understand existing source code and shared utilities.

1. Study @IMPLEMENTATION_PLAN.md (if present; it may be incorrect) and use up to 500 subagents to study existing source code in `src/*` and compare it against `specs/*`. Use a subagent to analyze findings, prioritize tasks, and create/update @IMPLEMENTATION_PLAN.md as a bullet point list sorted in priority of items yet to be implemented. Ultrathink. Consider searching for TODO, minimal implementations, placeholders, skipped/flaky tests, and inconsistent patterns. Study @IMPLEMENTATION_PLAN.md to determine starting point for research and keep it up to date with items considered complete/incomplete using subagents.

IMPORTANT: Plan only. Do NOT implement anything. Do NOT assume functionality is missing; confirm with code search first.

ULTIMATE GOAL: Build ralph-cli — a Node.js/TypeScript CLI tool (publishable to npm as `ralph-cli`) that prepares and maintains repositories for AI agent development. Commands include: `ralph init`, `ralph lint`, `ralph grade`, `ralph gc`, `ralph doctor`, `ralph plan`, `ralph promote`, `ralph ref`, `ralph hooks`, and `ralph ci`. The tool must be LLM-agnostic — no references to specific AI providers, model names, or context window sizes. Generated repo structures follow OpenAI's proven harness engineering patterns. Consider the dependency order: configuration and repo-scaffolding first, then commands that depend on that structure. If an element is missing, search first to confirm it doesn't exist, then if needed author the specification at specs/FILENAME.md. If you create a new element then document the plan to implement it in @IMPLEMENTATION_PLAN.md using a subagent.
